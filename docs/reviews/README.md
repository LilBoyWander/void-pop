# Reviews

Independent feature-level testing, security (when triggered), documentation,
and QC review records may be indexed here. The feature's primary evidence
remains in its shared `tasks/<timestamp>__<slug>/` folder.

Reviews must cite actual source, diff, command output, or captured evidence and
must separate verified facts from assumptions and unresolved gaps.
